{
  "bishop": {
    "files": {
      "ls": "ls",
      "listDetails": "ls -al"
    },
    "prod": {
      "_user": "theUser",
      "_server": "theServer",
      "secure": {
        "_identity":"file.id_rsa",
        "copy":"scp -i $identity $user@$server",
        "shell":"ssh -i $identity $user@$server"
      }
    },
    "underscore_test": {
      "someCommands": "echo 'hello'"
    },
    "minus-test": "ls -al",
    "chained": {
      "_chainedCommand": "echo -n",
      "command": {
        "test": "chained > /dev/null"
      }
    }
  },
  "foo": {
    "bar": "echo 'bar'"
  }
}
